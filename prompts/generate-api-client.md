You are a senior integration engineer. Write a complete and production-ready API client SDK file based on the following OpenAPI specification.

Context/Constraints:
- Language: {{LANGUAGE}}
- HTTP client/library to use: {{LIBRARY}} (e.g. fetch, axios, requests)

OpenAPI / Swagger Spec:
```json
{{OPENAPI_SPEC}}
```

Respond with:
1. A brief overview of the generated client class and methods.
2. The complete code block for the client file. Include appropriate types, headers handling, and error response mapping.
3. A short example of how to import and use the client in user code.
