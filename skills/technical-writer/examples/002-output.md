# Configuration

## Environment Variables

- `API_KEY` (Required): The API token for authentication.
- `PORT` (Optional): The network port (default: `8080`).

## Running the App

```bash
npm start
```