<inferred_schema>
const routes = [
  { path: '/', component: Home },
  { path: '/dashboard', component: Dashboard }
];
</inferred_schema>
<reproduction_steps>
1. Configure route mappings.
2. Render list elements dynamically inside navbar wrapper.
</reproduction_steps>